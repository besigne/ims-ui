export interface User {
  id: number,
  username: string,
  first_name: string,
  email: string,
  is_staff: boolean,
  is_active: boolean,
  last_login: string,
  date_joined: string,
  container_port: string
}